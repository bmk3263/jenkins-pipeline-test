pipeline{
    agent any 
   parameters {
    
       string(name: "omircon")
  }
    stages{
        stage('clone'){
                     
         steps{
               echo "${params.name}"
         }

         }
        
        
      }
}
